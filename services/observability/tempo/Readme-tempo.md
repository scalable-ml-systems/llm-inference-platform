# Tempo (optional)
Goal: trace request path across gateway -> router -> backend.

MVP later:
- instrument router with OpenTelemetry
- deploy Tempo via helm
- add Grafana datasource + exemplars from Prometheus histograms
