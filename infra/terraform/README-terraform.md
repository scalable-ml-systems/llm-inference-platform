Terraform manages the lifecycle of cloud infrastructure for the inference platform.
This includes Kubernetes clusters, node groups, networking, and supporting services.

Model deployments and runtime configuration are handled separately via Helm.
